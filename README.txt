
Can use MIT/GNU Scheme to follow along with the book. To install on Ubuntu use

sudo apt-get install mit-scheme

This comes with a built in emacs-like editor called Edwin.

Or use DrRacket. You can then install the sicp package and specify '#lang sicp' in source files. (This is what I ended up doing.)

